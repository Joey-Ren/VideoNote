# ============================================
# VideoNote 后端配置
# 复制此文件为 .env 并填入你的配置
# ============================================

# OpenAI 配置
OPENAI_API_KEY=your-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o

# Whisper 转录模型大小
# 可选: tiny, base, small, medium, large-v3
WHISPER_MODEL_SIZE=base

# YouTube API（可选，配置后加速 YouTube 视频预览）
YOUTUBE_API_KEY=

# yt-dlp Cookie 配置（B站等需要登录的平台）
# 方式一：从浏览器自动提取（推荐），可选: chrome/firefox/edge/safari，留空禁用
COOKIES_FROM_BROWSER=chrome
# 方式二：手动指定 cookies.txt 文件路径（优先级高于浏览器提取）
COOKIES_FILE=

# 应用配置
APP_HOST=0.0.0.0
APP_PORT=8000
TEMP_DIR=./temp
